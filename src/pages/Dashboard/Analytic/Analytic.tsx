import { Search } from '@rsuite/icons'
import { IconButton } from 'rsuite'

const Page = () => {
  return (
    <IconButton icon={<Search />} appearance="primary">
      d
    </IconButton>
  )
}

export default Page
